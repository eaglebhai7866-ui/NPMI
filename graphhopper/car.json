{
  "priority": [
    { "if": "road_class == MOTORWAY", "multiply_by": 1.0 },
    { "if": "road_class == PRIMARY", "multiply_by": 0.9 },
    { "if": "road_class == SECONDARY", "multiply_by": 0.8 },
    { "if": "road_class == TERTIARY", "multiply_by": 0.7 },
    { "if": "road_access == DESTINATION", "multiply_by": 0.1 }
  ],
  "speed": [
    { "if": "road_class == MOTORWAY", "limit_to": 120 },
    { "if": "road_class == PRIMARY", "limit_to": 80 },
    { "if": "road_class == SECONDARY", "limit_to": 60 },
    { "if": "road_class == TERTIARY", "limit_to": 40 }
  ]
}
